/**
 * progeree controller
 */
(function ( $ ) {
    $(document).ready(function() {
        NProgress.start();
    });

    $(window).load(function() {
        NProgress.done();
    });

})( jQuery );